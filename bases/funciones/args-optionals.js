"use strict";
(function () {
    var fullName = function (firstName, lastName) {
        return firstName + ", " + lastName;
    };
    var name = fullName('Elias', 'Garcia');
    console.log(name);
})();
